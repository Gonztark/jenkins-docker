pipeline {
    agent any

    stages {
        stage('Deploy') {
            steps {
                sh 'ls /var/jenkins_home/workspace/Pipeline-docker/docker/'
                sh 'ls /var/'
            }
        }
    }
}